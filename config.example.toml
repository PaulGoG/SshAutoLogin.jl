# Configuration file for automated SSH terminal tab orchestration

[globals]
# SSH connection timeout in seconds
connect_timeout = 10  # integer > 0; units: s

# SSH host key verification policy
strict_host_key_checking = "accept-new"  # one of: "accept-new" | "yes" | "no"

# OpenSSH log verbosity level
log_level = "ERROR"  # one of: "QUIET" | "FATAL" | "ERROR" | "INFO" | "VERBOSE" | "DEBUG"

[terminal]
# Target terminal emulator executable
emulator = "konsole"  # one of: "konsole"

# Window aggregation mode
mode = "tabs"  # one of: "tabs" | "windows"

# Retain terminal tab after SSH process exits
hold = false  # one of: true | false

[[targets]]
# Target hostname or IPv4/IPv6 address
host = "192.168.1.100"

# SSH daemon port number
port = 22  # integer in range [1, 65535]

# Remote username
user = "admin"

# Authentication password
password = "example_password_1"

# Tab or window display title (optional)
title = "Cluster Node 01"

# Target-specific host key checking policy override (optional)
strict_host_key_checking = "accept-new"  # one of: "accept-new" | "yes" | "no"

[[targets]]
# Target hostname or IPv4/IPv6 address
host = "192.168.1.101"

# SSH daemon port number
port = 2222  # integer in range [1, 65535]

# Remote username
user = "developer"

# Authentication password
password = "example_password_2"

# Tab or window display title (optional)
title = "Dev Gateway"
